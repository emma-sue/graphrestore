# Paper Story

Composite restoration lies between a rigid serial tool chain and an unconditional one-shot mixture. GraphRestore represents restoration as a partial-order program, applies named latent skills only where continuous guards say they are needed, and calibrates misuse through clean/wrong-skill counterfactual episodes.

